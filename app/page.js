import Typography from '@mui/material/Typography';

export default function HomePage() {
  return (
    <main>
      <Typography variant="h4" component="h1">Kitchen Sink Webapp</Typography>
      <Typography>Kitchen-sink template webapp — subtract features per client project.</Typography>
    </main>
  );
}
